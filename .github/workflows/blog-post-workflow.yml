name: Latest blog post workflow
on:
  schedule: # Run workflow automatically
    - cron: '0 * * * *' # Runs every hour, on the hour
  workflow_dispatch: # Run workflow manually (without waiting for the cron to be called), through the GitHub Actions Workflow page directly

jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Pull in dev.to posts
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          feed_list: "https://afflift.com/f/forums/-/index.rss,http://www.bbc.co.uk/zhongwen/simp/index.xml,https://www.voachinese.com/api/zm_yqebbyi,https://rss.dw.com/rdf/rss-chi-all,https://cn.nytimes.com/rss/,https://51.ruyo.net/feed,https://sspai.com/feed,"
          max_post_count: 100

